import React from 'react';
import styled from 'styled-components';

const Subtitle = styled.p`
  color: #e1010b;
  font-size: 0.75rem;
  margin-left: 0.4167em;
  padding-top: 0.25em;
  font-weight: 500;
`;

export default () => <Subtitle>viajar es la guita mejor invertida</Subtitle>;
